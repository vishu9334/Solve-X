{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "if [ -d \"xClient\" ]; then npm --prefix xClient install; else npm install; fi",
  "buildCommand": "if [ -d \"xClient\" ]; then npm --prefix xClient run build; else npm run build; fi",
  "outputDirectory": "xClient/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}


